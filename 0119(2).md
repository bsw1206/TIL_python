# GUI
- 사진이 있는 메뉴판을 보고 손가락으로 주문하는 방식
- 장점 : 쉽고 직관적이다
- ex. 마우스로 아이콘을 클릭하는 윈도우
- 그래픽을 통해 사용자와 컴퓨터가 상호 작용하는 방식
# CLI
- 주방장에게 직접 말로 주문하는 방식
- 장점 : 원하는 대로 정확하고, 세밀하며, 빠르게 요청할 수 있다.
- ex. Terminal, cmd
- 명령어를 통해 사용자와 컴퓨터가 상호 작용하는 방식

# 기초 문법
- touch : 파일 생성
- mkdir : 새 디렉토리 생성
- ls : 현재 작업 중인 디렉토리 내부의 폴더/파일 목록을 출력
- cd : 현재 작업 중인 디렉토리를 변경 (위치 이동)
- start : 폴더/파일을 열기
- rm : 파일 삭제

### 경로
- 한 건물 안에서 특정 방(디렉토리)이나 물건(파일)을 찾아가는 '주소'
- 절대 경로 : 정문까지의 전체 주소, 누가 어딜 보든 항상 똑같은 주소
- 상대 경로 : 현재 내 위치를 기준으로 한 주소 

# Git 
- 분산 버전 관리 시스템
- 변경 사항만 등록이 가능하다.
- 분산식으로 등록되어 여러 사용자가 파일을 한꺼번에 받아 사용 가능
- 역할 : 코드 버전을 관리, 개발되어 온 과정 파악, 이전 버전과의 변경 사항 비교

## Git의 3가지 영역
- Working Directory : 실제 작업중인 파일들이 위치하는 영역
- Staging Area : working directory에서 변경된 파일 중 다음 버전에 포함시킬 파일들을 선택적으로 추가하거나 제외할 수 있는 중간 준비 영역
- Repository : 버전 이력과 파일들이 영구적으로 저장되는 영역

**- commit** : 변경된 파일들을 저장하는 행위, snapshot이라고도 함.

## Git의 동작
- git init : 로컬 저장소 설정(초기화)
- git add : 변경사항이 있는 파일을 staging area에 추가
- git commit : staging area에 있는 파일들을 저장소에 기록
- git config --global user.email "메일주소"
- git config --global user.name "유저네임 : commit 작성자 정보 설정
- git log : commit history 보기
- git status : 현재 로컬 저장소의 파일 상태 보기
- git log --oneline : commit 목록 한 줄로 보기




---
---
---
# 확인 문제 
1. d이다. 메뉴판을 주문하는 것이 아니라 주방장에게 명령하는 방식과 유사함.
2. 절대 경로를 출력하는 것은 pwd a이다.
3. .. : 상위 디렉토리
~ 터미널 처음 키면 나타나는 디렉토리
. : 현재 작업중인 디렉토리
/ : 절대적인 초기 디렉토리 
c이다.
4. 인터넷이 연결되지 않아도 사용 가능
코드의 변화를 기록하고 추적하는 도구 o -> 답은 b
중앙 집중식이 아니라 분산 버전 관리 시스템
파일간의 변경사항을 기록함
5. 다음 버전에 포함시킬 파일들을 선택적으로 추가하는 중간 준비 영역은 staging area이다. 답 a
6. 새로운 git 로컬 저장소를 설정하기 위해 입력하는 명령어는 git init이다
git commit은 새로운 commit을 제작하는 도구
git status는 현재 staging area의 상태를 확인하는 도구
git add는 변경사항을 staging area에 추가하는 도구 : 답 a
7. 특정 시점의 버전을 기록하는 행위? commit이라 한다 답 d
8. 현재 로컬 저장소에서 파일들의 상태를 확인하기 위한 명령어 ? git status이다.
9. git을 처음 사용시 사용자 이름을 컴퓨터 전역에 설정하는 용어는 git commit global --user "name"인가? X 답은 git config --global user .name "이름"이다.
10 모든 파일을 staging area에 추가하는 명령어는 git add . 이다.